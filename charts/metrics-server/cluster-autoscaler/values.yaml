autoDiscovery:
  clusterName: awesome
  enabled: true
awsRegion: ap-southeast-1
cloudProvider: aws
rbac:
  create: true

serviceMonitor:
  enabled: true
  interval: 10s
  namespace: monitoring
  selector:
    release: prom